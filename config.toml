# The URL the site will be built for
base_url = "https://blog.jeffsmits.net"

# The site title and description; used in feeds by default.
title = "Whatever"
description = "A web log. Mostly about computer science-y stuff."

# The site theme to use.
theme = "zola-bearblog"

# The default language; used in feeds.
default_language = "en"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Files to ignore among co-located asset files
ignored_content = ["*.drawio", "*.drawio.bkp"]

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# When set to "true", a feed is automatically generated.
generate_feed = true

# The filename to use for the feed. Used as the template filename, too.
# Defaults to "atom.xml", which has a built-in template that renders an Atom 1.0 feed.
# There is also a built-in template "rss.xml" that renders an RSS 2.0 feed.
feed_filename = "rss.xml"

# The taxonomies to be rendered for the site and their configuration of the default languages
taxonomies = [
  {name = "tags"},
]

[translations]
no_posts = "No posts yet"
remove_filter = "Remove filter"
filtering_for = "Filtering for"
made_with = "Made with"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "ayu-dark"

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true
# Whether text between $$ should be interpreted as LaTeX and rendered to mathml with
# `latex2mathml`.
math = true
# Whether footnotes are rendered at the bottom in the style of GitHub
bottom_footnotes = true

# Various slugification strategies
[slugify]
# Whether to remove date prefixes for page path slugs.
paths_keep_dates = false

[extra]
# Put all your custom variables here
language_switcher = false

[extra.table_of_contents]
show = true
max_level = 3
